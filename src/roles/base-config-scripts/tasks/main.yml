---

- name: Ensure config variables available in PHP and shell files
  template:
    src: "config.{{ item }}.j2"
    dest: "{{ m_config_deploy_dir }}/config.{{ item }}"
    owner: root
    group: root
    mode: '0755'
  with_items:
    - "php"
    - "sh"

- name: Create wiki configuration file for PHP scripts
  template:
    src: "wiki-config.php.j2"
    dest: "{{ m_config_deploy_dir }}/wiki-config.php"
    owner: root
    group: root
    mode: '0644'
